{
  "name": "gmail-outlook-survey",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "node ./scripts/build.mjs",
    "deploy": "gh-pages -d dist",
    "config": "node ./scripts/generate-config.mjs",
    "dev": "cd src && python3 -m http.server 8080"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0"
  }
}
