{
  "name": "gmail-outlook-survey",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "node ./scripts/build.mjs",
    "deploy": "gh-pages -d dist"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0"
  }
}
